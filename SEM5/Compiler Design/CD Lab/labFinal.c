// practice file handling properly
// all functions
// everything



#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

FILE* input

int main(){
  
}

/*

FIRST TRY FOR BASIC ONES or THEN DO FOR THE REST OF THE CASES

1. Declare/Initialize keywords, sp syms, ...
2. Write conditions for their occurences. isKeyword, isSpSym, ... (func arguments name, )
3. Create a token structure (contains token name, type), and Symbol Table (if necessary)
4. fillToken() function {IMP FUNCTION} (SAVE TOKEN TYPE & NAME) [ARGUMENTS - TOKEN TYPE AND NAME]
5. getNextToken() function {IMP FUNCTION} [RETURNS TOKEN - AFTER FILLTOKEN]

6. Parser gets next Token, & then checks & verifies with the given grammar/production.
7. If it is a ID put it in the symbol table, (IN PARSER code).

*/